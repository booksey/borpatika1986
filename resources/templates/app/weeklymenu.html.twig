{% extends "layout.html.twig" %}
{% block main %}
    <section id="main-content">
        <div class="container-md" data-aos="fade-in">
            <div class="d-flex flex-row justify-content-end">
                <div class="fb-share-button" data-href="{{ ogUrl }}" data-layout="button_count"></div>
            </div>
            <div class="d-flex flex-row justify-content-center">
                <div>
                    <h1 class="text-center mb-5">
                        Heti menü
                        {{ weekNumber }}. hét - 1890 Ft / nap
                    </h1>
                    <h4 class="text-center">Napi menü 12:00-tól</h4>
                    {% for day, menus in weeklyMenu %}
                        <h2 class="text-center mt-5">
                            {{ day }}
                        </h2>
                        {% for menu in menus %}
                            <h3 class="text-center">
                                {{ menu }}
                            </h3>
                        {% endfor %}
                    {% endfor %}
                    <br/>
                    <h3 class="text-center mt-5">Jó étvágyat kívánunk!</h3>
                    <h4 class="text-center">Az allergén összetevőkről érdeklődjön munkatársainknál.</h4>
                    <h4 class="text-center">A menüváltoztatás jogát fenntartjuk.</h4>
                </div>
            </div>

        </div>
    </section>
{% endblock %}
